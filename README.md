# 🎯 ML-Web: Сервис анализа тональности обращений горожан

[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.111-green.svg)](https://fastapi.tiangolo.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

> Автоматический анализ тональности обращений горожан с использованием машинного обучения

---

## 📋 Содержание

- [Цель проекта](#-цель-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Структура проекта](#-структура-проекта)
- [API документация](#-api-документация)
- [ML-модели](#-ml-модели)
- [Фронтенд](#-фронтенд)
- [База данных](#-база-данных)
- [Docker и деплой](#-docker-и-деплой)
- [Makefile команды](#-makefile-команды)
- [Конфигурация](#%EF%B8%8F-конфигурация)
- [Тестирование](#-тестирование)
- [Расширения](#-возможные-расширения)

---

## 🎯 Цель проекта

Город получает большое количество отзывов и обращений жителей. Для быстрого реагирования на жалобы и эффективного распределения ресурсов требуется автоматический анализ тональности сообщений.

**Возможности системы:**
- ✅ Ввод текста обращения через веб-интерфейс
- ✅ Классификация тональности: `positive`, `neutral`, `negative`
- ✅ Отображение вероятностей по каждому классу
- ✅ Пакетная обработка CSV-файлов
- ✅ Статистика и графики
- ✅ Сбор обратной связи для дообучения модели

---

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- [uv](https://github.com/astral-sh/uv) (рекомендуется) или pip

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/your-username/ML-Web.git
cd ML-Web

# Установка uv (если не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh  # Linux/macOS
# или
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"  # Windows

# Установка зависимостей
uv sync
```

### Запуск

```bash
# Деплой (применение миграций БД)
make deploy

# Запуск сервера
uv run uvicorn backend.app.main:app --host 0.0.0.0 --port 8000

# Или через Makefile
make serve
```

Откройте браузер: **http://127.0.0.1:8000/ui**

### Docker

```bash
docker compose up --build
```

---

## 📁 Структура проекта

```
ML-Web/
├── 📂 backend/
│   └── app/
│       ├── main.py           # FastAPI приложение
│       ├── model.py          # Загрузка ML-модели
│       ├── database.py       # SQLAlchemy модели и подключение
│       ├── schemas.py        # Pydantic-схемы
│       ├── config.py         # Конфигурация приложения
│       ├── stats.py          # Трекер статистики
│       ├── feedback.py       # Хранилище обратной связи
│       └── reports.py        # Загрузчик отчётов
├── 📂 frontend/
│   ├── index.html            # Веб-интерфейс
│   ├── styles.css            # Стили
│   └── app.js                # JavaScript логика
├── 📂 ml/
│   ├── train_baseline.py     # Обучение TF-IDF + LogReg
│   ├── train_transformer.py  # Обучение трансформера
│   ├── evaluate_model.py     # Оценка модели
│   ├── eda.py                # Разведочный анализ
│   ├── history_report.py     # Генерация отчёта истории
│   └── feedback_to_dataset.py # Экспорт фидбэка в CSV
├── 📂 data/
│   ├── app.db                # SQLite база данных
│   ├── sample_reviews.csv    # Демо-датасет
│   └── sample_batch.csv      # Пример для пакетной обработки
├── 📂 models/
│   ├── baseline.joblib       # Обученная baseline модель
│   ├── metadata.json         # Метаданные модели
│   └── transformer/          # Трансформер модель (после обучения)
├── 📂 reports/
│   ├── eval_metrics.json     # Метрики оценки
│   └── history_summary.json  # Сводка истории
├── 📂 alembic/
│   └── versions/             # Миграции базы данных
├── 📂 scripts/
│   ├── deploy.py             # Скрипт деплоя
│   ├── deploy.sh             # Bash скрипт деплоя
│   ├── deploy.ps1            # PowerShell скрипт деплоя
│   └── docker-entrypoint.sh  # Entrypoint для Docker
├── 📂 tests/                 # Модульные тесты
├── pyproject.toml            # Зависимости проекта
├── alembic.ini               # Конфигурация Alembic
├── docker-compose.yml        # Docker Compose
├── Dockerfile                # Docker образ
└── Makefile                  # Команды автоматизации
```

---

## 📡 API документация

### Основные эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/health` | Health-check сервиса |
| `POST` | `/predict` | Классификация одного текста |
| `POST` | `/predict_batch` | Пакетная классификация списка |
| `POST` | `/predict_file` | Загрузка и классификация CSV |
| `GET` | `/stats` | Статистика предсказаний |
| `GET` | `/model` | Метаданные модели |
| `GET` | `/reports/metrics` | Метрики качества модели |
| `GET` | `/reports/history` | Сводка истории предсказаний |
| `POST` | `/feedback` | Отправка обратной связи |
| `GET` | `/feedback` | Получение записей фидбэка |

### Примеры запросов

**Классификация текста:**
```bash
curl -X POST http://localhost:8000/predict \
  -H "Content-Type: application/json" \
  -d '{"text": "Отличный сервис, все работает быстро!"}'
```

**Ответ:**
```json
{
  "label": "positive",
  "scores": {
    "positive": 0.89,
    "neutral": 0.08,
    "negative": 0.03
  },
  "model_name": "transformer (rubert-tiny)"
}
```

**Пакетная обработка CSV:**
```bash
curl -X POST -F "file=@data/sample_batch.csv" http://localhost:8000/predict_file
```

📖 **Swagger UI:** http://localhost:8000/docs  
📖 **ReDoc:** http://localhost:8000/redoc

---

## 🤖 ML-модели

### Baseline (TF-IDF + Logistic Regression)

```bash
# Обучение
make train

# Результат: models/baseline.joblib, models/metadata.json
```

### Трансформер (RuBERT)

```bash
# Обучение (требуется GPU)
make train-transformer

# Параметры по умолчанию:
# - Модель: cointegrated/rubert-tiny
# - Эпохи: 3
# - Batch size: 16

# Результат: models/transformer/
```

### Оценка качества

```bash
make evaluate
# Результат: reports/eval_metrics.json
```

**Метрики:**
- Accuracy
- Macro F1-score
- Classification report
- Confusion matrix

### Fallback модель

При отсутствии обученной модели автоматически используется `KeywordFallbackModel` — простой классификатор на основе ключевых слов.

---

## 🖥️ Фронтенд

Чистый HTML/CSS/JS без сборщиков — доступен по адресу `/ui`.

**Возможности:**
- 📝 Ввод текста и мгновенная классификация
- 📊 Doughnut-график распределения классов
- 📋 История последних запросов
- 📁 Пакетная загрузка CSV
- 💬 Форма обратной связи
- 📈 Панель метрик модели

---

## 🗄️ База данных

Проект использует **SQLite** с **SQLAlchemy** и **Alembic** для миграций.

### Таблицы

- `prediction_records` — история предсказаний
- `feedback_entries` — записи обратной связи

### Миграции

```bash
# Применение миграций
uv run alembic upgrade head

# Создание новой миграции
uv run alembic revision --autogenerate -m "Description"

# Откат миграции
uv run alembic downgrade -1
```

### Расположение БД

По умолчанию: `data/app.db`

Можно изменить через переменную окружения:
```bash
export APP_DATABASE_PATH=/path/to/database.db
```

---

## 🐳 Docker и деплой

### Docker Compose

```bash
# Сборка и запуск
docker compose up --build

# Остановка
docker compose down
```

### Traefik интеграция

В `docker-compose.yml` настроены labels для Traefik:
```yaml
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.api.rule=Host(`ml.example.com`)"
  - "traefik.http.services.api.loadbalancer.server.port=8000"
```

### Скрипты деплоя

```bash
# Python скрипт (рекомендуется)
uv run python scripts/deploy.py

# Bash (Linux/macOS)
./scripts/deploy.sh

# PowerShell (Windows)
.\scripts\deploy.ps1

# Через Makefile
make deploy
```

**Скрипт деплоя выполняет:**
1. Проверку необходимых директорий
2. Применение миграций базы данных
3. Проверку подключения к БД

---

## 🛠️ Makefile команды

| Команда | Описание |
|---------|----------|
| `make install` | Установка зависимостей (`uv sync`) |
| `make deploy` | Запуск скрипта деплоя |
| `make serve` | Запуск сервера разработки |
| `make train` | Обучение baseline модели |
| `make train-transformer` | Обучение трансформера |
| `make evaluate` | Оценка модели |
| `make eda` | Разведочный анализ данных |
| `make history-report` | Генерация отчёта истории |
| `make feedback-export` | Экспорт фидбэка в CSV |
| `make test` | Запуск тестов |
| `make docker-up` | Запуск Docker контейнеров |
| `make docker-down` | Остановка Docker контейнеров |

---

## ⚙️ Конфигурация

Настройки хранятся в `backend/app/config.py` и могут быть переопределены через переменные окружения с префиксом `APP_`.

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `APP_MODEL_PATH` | `models/baseline.joblib` | Путь к baseline модели |
| `APP_TRANSFORMER_DIR` | `models/transformer` | Путь к трансформеру |
| `APP_USE_TRANSFORMER` | `true` | Использовать трансформер |
| `APP_DATABASE_PATH` | `data/app.db` | Путь к SQLite БД |
| `APP_FRONTEND_DIR` | `frontend` | Путь к статике |
| `APP_ALLOW_ORIGINS` | `["*"]` | CORS origins |

**Пример `.env` файла:**
```env
APP_USE_TRANSFORMER=true
APP_DATABASE_PATH=data/production.db
APP_ALLOW_ORIGINS=["https://example.com"]
```

---

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# Или напрямую
uv run python -m unittest discover -s tests -p 'test_*.py'
```

**Покрытие тестами:**
- `StatsTracker` — трекер статистики
- `FeedbackStore` — хранилище фидбэка
- `ReportLoader` — загрузчик отчётов
- `SentimentModel` — fallback модель

---

## 🔮 Возможные расширения

- 🏷️ **Аспектная классификация** — анализ по категориям (интерфейс, скорость, удобство)
- 📂 **Тематическая классификация** — транспорт, ЖКХ, медицина, образование
- 🔄 **Active Learning** — интерфейс для исправления ошибок и дообучения
- 🔍 **Explainability** — подсветка ключевых фраз (LIME, SHAP, attention)
- 📊 **BI-интеграция** — экспорт в Tableau, Power BI
- 🔔 **Алерты** — уведомления о негативных обращениях

---

## 📄 Лицензия

MIT License — см. [LICENSE](LICENSE)

---

## 👥 Команда

| Участник | Роль | Зона ответственности |
|----------|------|----------------------|
| **Махмудов Мердан** | Data Scientist | Данные, EDA, обучение моделей, метрики |
| **Носков Владислав** | Backend Developer | API, интеграция с моделью, деплой |
| **Енин Дмитрий** | Frontend Developer | UI, интеграция с API |
| **Васильченко Мария** | Frontend Developer | UI, вёрстка, адаптив |
| **Шмелев Артем** | UX/UI Designer | Прототипы, дизайн, user flow |

---

<p align="center">
  <b>⭐ Если проект полезен, поставьте звезду!</b>
</p>
